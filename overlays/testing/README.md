# Testing Configuration Overlay

This overlay configuration provides configuration specific to things that are currently in development.

These configs are not meant for general consumption, and are generally considered **work-in-progress**.  Attempting to use these may cause the `build` script to fail, or images that do not work.
