# Offical Alpine Configuration Overlay

This overlay configuration provides configuration specific for building, storing, publishing, and releasing official Alpine Linux cloud images.

These configs are not meant for general consumption.  Attempting to use these may cause the `build` script to fail due to lack of necessary credentials.
